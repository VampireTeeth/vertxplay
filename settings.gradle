/*
 * This settings file was auto generated by the Gradle buildInit task
 * by 'sliu11' at '13/03/17 10:31 AM' with Gradle 3.1
 *
 * The settings file is used to specify which projects to include in your build.
 * In a single project build this file can be empty or even removed.
 *
 * Detailed information about configuring a multi-project build in Gradle can be found
 * in the user guide at https://docs.gradle.org/3.1/userguide/multi_project_builds.html
 */

/*
// To declare projects as part of a multi-project build use the 'include' method
include 'shared'
include 'api'
include 'services:webservice'
*/


pluginRepositories {
  maven {
    url "${nexusRepoUrl}/caf-releases"
    credentials {
      username "$nexusUsername"
      password "$nexusPassword"
    }
  }
  maven {
    url "${nexusRepoUrl}/bfs-releases"
    credentials {
      username "$nexusUsername"
      password "$nexusPassword"
    }
  }
  maven {
    url "${nexusRepoUrl}/public"
    credentials {
      username "$nexusUsername"
      password "$nexusPassword"
    }
  }
  maven {
    url "${nexusRepoUrl}/public-release"
    credentials {
      username "$nexusUsername"
      password "$nexusPassword"
    }
  }
}

rootProject.name = 'vertxplay'
